<!-- rounded_border.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/cardTextColor" />
    <corners android:radius="20dp" /> <!-- Adjust the radius value as per your preference for curve amount -->
    <padding
        android:left="2dp"
        android:right="2dp"
        android:top="2dp"
        android:bottom="2dp" />
</shape>
