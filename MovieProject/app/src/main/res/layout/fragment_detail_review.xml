<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".view.DetailReviewFragment"
    android:paddingTop="35dp">
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/no_reviews_found"
        android:textSize="20sp"
        android:textColor="@color/light_theme"
        android:layout_gravity="center"
        android:visibility="gone"
        android:id="@+id/tv_detail_review_no_reviews_found" />


    <!-- TODO: Update blank fragment layout -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_detail_review"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/detailFragmentCardViewMargin" />

</FrameLayout>