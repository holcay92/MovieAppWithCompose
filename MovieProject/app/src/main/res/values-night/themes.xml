<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!-- Night theme -->
    <style name="Base.Theme.MovieApp.Night" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize colors for night mode. -->
        <item name="colorPrimary">@color/main_theme_night</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/secondary_theme_night</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/status_bar_color</item>
    </style>

    <style name="Theme.MovieApp.Night" parent="Base.Theme.MovieApp.Night" />

    <!-- You can define additional styles for night mode if needed. -->
</resources>
